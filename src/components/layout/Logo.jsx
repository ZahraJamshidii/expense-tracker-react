function Logo() {
  return (
    <h1
      className="text-2xl font-extrabold tracking-wide transition-all duration-300"
      style={{
        color: "var(--primary)",
      }}
    >
      Expense Tracker
    </h1>
  );
}

export default Logo;